#include "main.h"
/**
 * mul - checks for the multiplication of 2 integers
 * @a: an integer to be multiplied by b
 * @b: an integer to be multiplied by a
 * Return: return the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
